<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jawad Dental | عيادة طب الأسنان</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* CSS Variables */
        :root {
            --primary-blue: #1F5FBF;
            --primary-turquoise: #19B6D1;
            --light-gray: #f5f7fa;
            --dark-text: #2d3748;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            color: var(--dark-text);
            background-color: #fff;
            overflow-x: hidden;
            line-height: 1.6;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header & Navigation - Improved Design */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background-color: rgba(255, 255, 255, 0.95);
            transition: var(--transition);
            padding: 15px 0;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(31, 95, 191, 0.1);
        }

        header.scrolled {
            box-shadow: var(--shadow);
            padding: 10px 0;
            background-color: rgba(255, 255, 255, 0.98);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--primary-blue);
            display: flex;
            align-items: center;
            position: relative;
        }

        .logo i {
            margin-left: 8px;
            color: var(--primary-turquoise);
        }

        .logo:after {
            content: '';
            position: absolute;
            bottom: -5px;
            right: 0;
            width: 30px;
            height: 3px;
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            border-radius: 2px;
        }

        .nav-menu {
            display: flex;
            align-items: center;
            margin-right: auto;
            margin-left: 40px;
        }

        .nav-menu li {
            margin: 0 15px;
        }

        .nav-menu a {
            font-weight: 500;
            transition: var(--transition);
            position: relative;
            padding: 8px 5px;
        }

        .nav-menu a:after {
            content: '';
            position: absolute;
            bottom: 0;
            right: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            transition: var(--transition);
            border-radius: 2px;
        }

        .nav-menu a:hover {
            color: var(--primary-blue);
        }

        .nav-menu a:hover:after {
            width: 100%;
        }

        .cta-button {
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(31, 95, 191, 0.3);
            position: relative;
            overflow: hidden;
        }

        .cta-button:before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to left, var(--primary-turquoise), var(--primary-blue));
            opacity: 0;
            transition: var(--transition);
            z-index: 1;
        }

        .cta-button:hover:before {
            opacity: 1;
        }

        .cta-button span {
            position: relative;
            z-index: 2;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(31, 95, 191, 0.4);
        }

        .hamburger {
            display: none;
            cursor: pointer;
            width: 30px;
            height: 30px;
            position: relative;
        }

        .bar {
            display: block;
            width: 25px;
            height: 3px;
            margin: 5px auto;
            transition: var(--transition);
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            border-radius: 2px;
        }

        /* Hero Section - Improved Design */
        .hero {
            position: relative;
            height: 100vh;
            display: flex;
            align-items: center;
            background-color: white;
            overflow: hidden;
            padding-top: 80px;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(31, 95, 191, 0.05) 0%, rgba(25, 182, 209, 0.05) 100%);
            z-index: -1;
        }

        /* New Background Image with Overlay */
        .hero-bg-image {
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1544006650-7c2e5c6d5c8f?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1000&q=80');
            background-size: cover;
            background-position: center;
            opacity: 0.15;
            z-index: -2;
        }

        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
        }

        .hero-text {
            flex: 1;
            max-width: 600px;
            animation: fadeIn 1s ease forwards;
            position: relative;
            z-index: 5;
        }

        .hero-text h1 {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 20px;
            color: var(--primary-blue);
            line-height: 1.2;
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-text p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: var(--dark-text);
            max-width: 90%;
        }

        .hero-btn {
            display: inline-flex;
            align-items: center;
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            color: white;
            padding: 18px 40px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 1.2rem;
            transition: var(--transition);
            box-shadow: 0 4px 20px rgba(31, 95, 191, 0.4);
            position: relative;
            overflow: hidden;
        }

        .hero-btn:before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to left, var(--primary-turquoise), var(--primary-blue));
            opacity: 0;
            transition: var(--transition);
        }

        .hero-btn:hover:before {
            opacity: 1;
        }

        .hero-btn span {
            position: relative;
            z-index: 2;
        }

        .hero-btn i {
            margin-right: 15px;
            position: relative;
            z-index: 2;
            animation: bounce 2s infinite;
        }

        .hero-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(31, 95, 191, 0.5);
        }

        .trust-badges {
            display: flex;
            align-items: center;
            margin-top: 40px;
            flex-wrap: wrap;
            background: rgba(255, 255, 255, 0.8);
            padding: 15px 25px;
            border-radius: 15px;
            backdrop-filter: blur(5px);
            width: fit-content;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .stars {
            color: #FFD700;
            font-size: 1.4rem;
            margin-left: 15px;
            display: flex;
        }

        .reviews {
            font-weight: 700;
            color: var(--dark-text);
            background: var(--light-gray);
            padding: 8px 18px;
            border-radius: 20px;
            margin-right: 15px;
        }

        .hero-image {
            flex: 1;
            display: flex;
            justify-content: center;
            animation: slideIn 1s ease forwards;
            position: relative;
            z-index: 5;
        }

        .image-container {
            position: relative;
            width: 400px;
            height: 400px;
            border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
            border: 5px solid white;
            animation: morphing 10s ease-in-out infinite;
        }

        .image-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, rgba(31, 95, 191, 0.1), rgba(25, 182, 209, 0.1));
        }

        .floating-icon {
            position: absolute;
            z-index: 2;
        }

        .icon-1 {
            top: 20%;
            left: 10%;
            color: var(--primary-blue);
            font-size: 2rem;
            animation: float 6s ease-in-out infinite;
        }

        .icon-2 {
            top: 60%;
            right: 15%;
            color: var(--primary-turquoise);
            font-size: 1.8rem;
            animation: float 7s ease-in-out infinite;
        }

        .icon-3 {
            bottom: 20%;
            left: 20%;
            color: var(--primary-blue);
            font-size: 2.2rem;
            animation: float 5s ease-in-out infinite;
        }

        /* New Creative Elements */
        .circle-shape {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--primary-blue), var(--primary-turquoise));
            opacity: 0.1;
            z-index: 1;
        }

        .circle-1 {
            width: 300px;
            height: 300px;
            top: -150px;
            right: -150px;
        }

        .circle-2 {
            width: 200px;
            height: 200px;
            bottom: 50px;
            left: 50px;
        }

        .circle-3 {
            width: 150px;
            height: 150px;
            bottom: 200px;
            right: 200px;
        }

        .dental-icon {
            position: absolute;
            font-size: 24px;
            color: var(--primary-turquoise);
            opacity: 0.5;
            z-index: 1;
        }

        /* New Features */
        .appointment-bar {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: linear-gradient(to left, var(--primary-blue), var(--primary-turquoise));
            color: white;
            padding: 15px 0;
            text-align: center;
            z-index: 999;
            transform: translateY(100%);
            transition: transform 0.5s ease;
            box-shadow: 0 -4px 10px rgba(0, 0, 0, 0.15);
        }

        .appointment-bar.visible {
            transform: translateY(0);
        }

        .appointment-bar-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .appointment-text {
            font-weight: 500;
        }

        .appointment-btn {
            background: white;
            color: var(--primary-blue);
            border: none;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition);
        }

        .appointment-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        .stats-container {
            position: absolute;
            bottom: 30px;
            left: 30px;
            display: flex;
            gap: 20px;
            z-index: 5;
        }

        .stat-item {
            background: rgba(255, 255, 255, 0.9);
            padding: 15px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(5px);
            animation: fadeIn 1.5s ease forwards;
            animation-delay: 0.5s;
            opacity: 0;
        }

        .stat-number {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--primary-blue);
            margin-bottom: 5px;
        }

        .stat-text {
            font-size: 0.9rem;
            color: var(--dark-text);
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes float {
            0% {
                transform: translateY(0) rotate(0deg);
            }
            50% {
                transform: translateY(-20px) rotate(10deg);
            }
            100% {
                transform: translateY(0) rotate(0deg);
            }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateX(0);
            }
            40% {
                transform: translateX(-10px);
            }
            60% {
                transform: translateX(-5px);
            }
        }

        @keyframes morphing {
            0% {
                border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            }
            25% {
                border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%;
            }
            50% {
                border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%;
            }
            75% {
                border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%;
            }
            100% {
                border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            }
        }

        /* Particles */
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            background-color: rgba(31, 95, 191, 0.1);
            border-radius: 50%;
            animation: floatParticle 15s infinite linear;
        }

        @keyframes floatParticle {
            0% {
                transform: translateY(0) translateX(0) rotate(0deg);
            }
            100% {
                transform: translateY(-100vh) translateX(100px) rotate(360deg);
            }
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                padding: 40px 0;
            }

            .hero-text {
                max-width: 100%;
                margin-bottom: 40px;
            }

            .hero-text h1 {
                font-size: 2.8rem;
            }
            
            .hero-text p {
                max-width: 100%;
            }

            .trust-badges {
                justify-content: center;
                margin: 30px auto;
            }

            .image-container {
                width: 320px;
                height: 320px;
            }
            
            .nav-menu {
                margin-left: 20px;
            }
            
            .nav-menu li {
                margin: 0 10px;
            }
            
            .stats-container {
                position: relative;
                bottom: 0;
                left: 0;
                justify-content: center;
                width: 100%;
                margin-top: 30px;
            }
            
            .appointment-bar-content {
                flex-direction: column;
                gap: 10px;
            }
        }

        @media (max-width: 768px) {
            .hamburger {
                display: block;
            }

            .hamburger.active .bar:nth-child(2) {
                opacity: 0;
            }

            .hamburger.active .bar:nth-child(1) {
                transform: translateY(8px) rotate(45deg);
            }

            .hamburger.active .bar:nth-child(3) {
                transform: translateY(-8px) rotate(-45deg);
            }

            .nav-menu {
                position: fixed;
                right: -100%;
                top: 70px;
                gap: 0;
                flex-direction: column;
                background-color: white;
                width: 100%;
                text-align: center;
                transition: 0.3s;
                box-shadow: var(--shadow);
                padding: 20px 0;
                margin-left: 0;
            }

            .nav-menu.active {
                right: 0;
            }

            .nav-menu li {
                margin: 16px 0;
            }

            .hero-text h1 {
                font-size: 2.3rem;
            }

            .hero-text p {
                font-size: 1.1rem;
            }

            .image-container {
                width: 280px;
                height: 280px;
            }

            .circle-1, .circle-2, .circle-3 {
                display: none;
            }
            
            .hero-btn {
                padding: 15px 30px;
                font-size: 1.1rem;
            }
            
            .stat-item {
                padding: 10px;
            }
            
            .stat-number {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .hero-text h1 {
                font-size: 2rem;
            }

            .hero-btn {
                padding: 12px 25px;
                font-size: 1rem;
            }

            .image-container {
                width: 240px;
                height: 240px;
            }

            .logo {
                font-size: 1.5rem;
            }
            
            .trust-badges {
                flex-direction: column;
                gap: 10px;
            }
            
            .reviews {
                margin-right: 0;
            }
            
            .stats-container {
                flex-wrap: wrap;
            }
        }
    </style>
</head>
<body>
    <!-- Header & Navigation -->
    <header id="header">
        <div class="container nav-container">
            <a href="#" class="logo">
                Jawad Dental <i class="fas fa-tooth"></i>
            </a>
            
            <nav>
                <ul class="nav-menu">
                    <li><a href="#">الرئيسية</a></li>
                    <li><a href="#">خدماتنا</a></li>
                    <li><a href="#">آراء العملاء</a></li>
                    <li><a href="#">تواصل معنا</a></li>
                </ul>
            </nav>
            
            <button class="cta-button"><span>احجز الآن</span></button>
            
            <div class="hamburger">
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="hero">
        <div class="hero-bg"></div>
        <div class="hero-bg-image"></div>
        
        <!-- Animated Shapes -->
        <div class="circle-shape circle-1"></div>
        <div class="circle-shape circle-2"></div>
        <div class="circle-shape circle-3"></div>
        
        <!-- Animated Particles -->
        <div class="particles" id="particles"></div>
        
        <div class="container hero-content">
            <div class="hero-text">
                <h1>ابتسامتك تستاهل ثقة وراحة</h1>
                <p>لو سنانك مش زي ما بتحلم، أو عندك خوف من دكاترة الأسنان… جواد بيخلي التجربة مختلفة، آمنة، ومريحة.</p>
                <a href="#booking" class="hero-btn">
                    <span>ابدأ رحلتك للابتسامة المثالية</span>
                    <i class="fas fa-arrow-left"></i>
                </a>
                
                <div class="trust-badges">
                    <div class="reviews">أكتر من 500 مريض راضي</div>
                    <div class="stars">
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                    </div>
                </div>
            </div>
            
            <div class="hero-image">
                <div class="image-container">
                    <img src="https://images.unsplash.com/photo-1622253692010-333f2da6031d?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=400&q=80" alt="Patient smiling after dental treatment at Jawad Dental clinic">
                    <div class="image-overlay"></div>
                    
                    <!-- Floating Icons -->
                    <div class="floating-icon icon-1">
                        <i class="fas fa-tooth"></i>
                    </div>
                    <div class="floating-icon icon-2">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="floating-icon icon-3">
                        <i class="fas fa-heart"></i>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Stats Section -->
        <div class="stats-container">
            <div class="stat-item">
                <div class="stat-number">+500</div>
                <div class="stat-text">عملاء سعداء</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">15+</div>
                <div class="stat-text">سنوات خبرة</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">98%</div>
                <div class="stat-text">نجاح المعالجات</div>
            </div>
        </div>
    </section>
    
    <!-- Appointment Bar -->
    <div class="appointment-bar" id="appointmentBar">
        <div class="appointment-bar-content">
            <div class="appointment-text">احجز موعدك الآن واحصل على استشارة مجانية</div>
            <button class="appointment-btn">احجز الآن</button>
        </div>
    </div>

    <script>
        // Create floating particles
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particlesCount = 20;
            
            for (let i = 0; i < particlesCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random size between 5 and 15px
                const size = Math.floor(Math.random() * 10) + 5;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                
                // Random position
                particle.style.top = `${Math.random() * 100}vh`;
                particle.style.left = `${Math.random() * 100}vw`;
                
                // Random animation delay
                particle.style.animationDelay = `${Math.random() * 5}s`;
                
                // Random color (blue or turquoise with opacity)
                const colors = [
                    'rgba(31, 95, 191, 0.2)',
                    'rgba(25, 182, 209, 0.2)'
                ];
                particle.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                
                particlesContainer.appendChild(particle);
            }
        }
        
        // Create dental icons in background
        function createDentalIcons() {
            const heroSection = document.getElementById('hero');
            const iconsCount = 8;
            
            for (let i = 0; i < iconsCount; i++) {
                const icon = document.createElement('i');
                icon.className = 'dental-icon fas fa-tooth';
                
                // Random position
                icon.style.top = `${Math.random() * 100}vh`;
                icon.style.left = `${Math.random() * 100}vw`;
                
                // Random animation delay
                icon.style.animation = `float ${10 + Math.random() * 10}s ease-in-out infinite`;
                icon.style.animationDelay = `${Math.random() * 5}s`;
                
                // Random size
                const size = 16 + Math.floor(Math.random() * 20);
                icon.style.fontSize = `${size}px`;
                
                heroSection.appendChild(icon);
            }
        }
        
        // Sticky header on scroll
        function handleHeaderOnScroll() {
            const header = document.getElementById('header');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    header.classList.add('scrolled');
                } else {
                    header.classList.remove('scrolled');
                }
            });
        }
        
        // Show appointment bar on scroll
        function handleAppointmentBar() {
            const appointmentBar = document.getElementById('appointmentBar');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 300) {
                    appointmentBar.classList.add('visible');
                } else {
                    appointmentBar.classList.remove('visible');
                }
            });
        }
        
        // Mobile menu toggle
        function setupMobileMenu() {
            const hamburger = document.querySelector('.hamburger');
            const navMenu = document.querySelector('.nav-menu');
            
            hamburger.addEventListener('click', () => {
                hamburger.classList.toggle('active');
                navMenu.classList.toggle('active');
            });
            
            // Close menu when clicking on a link
            document.querySelectorAll('.nav-menu a').forEach(link => {
                link.addEventListener('click', () => {
                    hamburger.classList.remove('active');
                    navMenu.classList.remove('active');
                });
            });
        }
        
        // Smooth scrolling for navigation links
        function setupSmoothScrolling() {
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    
                    const targetId = this.getAttribute('href');
                    if (targetId === '#') return;
                    
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        }
        
        // Initialize all functions when DOM is loaded
        document.addEventListener('DOMContentLoaded', () => {
            createParticles();
            createDentalIcons();
            handleHeaderOnScroll();
            handleAppointmentBar();
            setupMobileMenu();
            setupSmoothScrolling();
        });
    </script>
</body>
</html>